The file contains the a 3D model of the Screw with its lenght, dimensions and sizes.
It contains the shapes like circles, polygon, cylinder
The function is used "Module", "translate", "echo".
Mainly used "if" (Condition).
